{
  "name": "cheat-sheet",
  "version": "1.1.0",
  "description": "Offensive Security & DevSecOps Command Reference — Penetration Testing & Cloud-Native Cheat Sheet",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test",
    "lint": "eslint . --max-warnings 0",
    "validate-content": "node scripts/validate-content.js",
    "update-readme": "node scripts/update-readme.js",
    "build:static": "node scripts/build-static.js",
    "tag:attack": "node scripts/tag-attack.js --report"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.0"
  },
  "//overrides": "express@4 pins these transitively at versions with published advisories (path-to-regexp ReDoS GHSA-37ch-88jc-xwx2 is high) and `npm audit fix` cannot lift them without leaving express behind. The pins are patch-level within the same major, so express keeps working; drop an entry once express ships a release that already requires the fixed version.",
  "overrides": {
    "path-to-regexp": "0.1.13",
    "body-parser": "1.20.8",
    "qs": "6.16.0"
  },
  "devDependencies": {
    "eslint": "^10.10.0"
  }
}
