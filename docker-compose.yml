services:
  cyberlens:
    build: .
    container_name: cyberlens
    ports:
      - "8899:3000"
    volumes:
      - cyberlens-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000

volumes:
  cyberlens-data:
