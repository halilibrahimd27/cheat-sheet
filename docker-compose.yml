services:
  cheat-sheet:
    build: .
    container_name: cheat-sheet
    ports:
      - "8899:3000"
    volumes:
      - cheat-sheet-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000

volumes:
  cheat-sheet-data:
